__pycache__/
*.pyc
out/
out_fleet/
.DS_Store
out_trace/
out_select/
out_runs/
out_app/
out_dash/
out_holistic/

# Build metadata regenerated by `pip install -e .` — tracking it means every
# install shows up as a diff.
*.egg-info/
build/
dist/

# The page, copied under the package by `web/build_blocks.py` so a wheel can
# ship it. `web/blocks.html` is the committed artifact; this is its build copy.
deepcompare/page/

# Output directories from the demo commands in the README.
out_*/

# The default output directory of `agentdiff eval` when no -o is given.
eval/
